Drop real NewSong photos here. See the image manifest in ../README.md for the
exact filenames each page expects. Until real images are added, the pages will
show broken image icons, which is expected.
